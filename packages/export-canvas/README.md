# @erbitron/loom-weave-kit-export-canvas

Browser PNG export support using `OffscreenCanvas` or `HTMLCanvasElement`.

## Usage

```ts
import { exportPNG } from '@erbitron/loom-weave-kit-export-canvas';
```
